{
  "name": "workpath-author-local",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run build -w @workpath/core && concurrently -k -n api,web -c blue,magenta \"npm run dev -w @workpath/server\" \"npm run dev -w @workpath/web\"",
    "launch": "node run-workpath.mjs",
    "build": "npm run build -w @workpath/core && npm run build -w @workpath/web && npm run build -w @workpath/server",
    "release:windows": "node scripts/build-portable-release.mjs",
    "start": "npm run start -w @workpath/server",
    "typecheck": "npm run build -w @workpath/core && npm run typecheck --workspaces --if-present",
    "test": "npm run test -w @workpath/core && npm run test -w @workpath/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
